<?xml version="1.0" encoding="utf-8"?>
<!-- M5 Phase 3.1 follow-on — Inter font family for notification typography.
     Bundles Regular (400) + Bold (700) only; mockup spec uses these two
     weights. `android:textStyle="bold"` on a TextView with this fontFamily
     auto-picks the 700 variant.

     Inter v4.1 (rsms/inter, OFL-1.1). TTFs at res/font/inter_regular.ttf
     and res/font/inter_bold.ttf. AndroidX fontWeight attribute used for
     API < 28; android:fontWeight added for API 28+. -->
<font-family xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <font
        android:fontStyle="normal"
        android:fontWeight="400"
        android:font="@font/inter_regular"
        app:fontStyle="normal"
        app:fontWeight="400"
        app:font="@font/inter_regular" />
    <font
        android:fontStyle="normal"
        android:fontWeight="700"
        android:font="@font/inter_bold"
        app:fontStyle="normal"
        app:fontWeight="700"
        app:font="@font/inter_bold" />
</font-family>
